int main()
{
    int arr[10];
/*
    int c = 5;
    int arr2[c];
*/

    arr[0] = 1;

    if (arr[0] != 1)
        return 1;

    for (int i = 0; i < 10; i++) {
        arr[i] = i * 2;
    }
    if (arr[0] == 1)
        return 2;
    if (arr[5] != 10)
        return 3;
    if (arr[8] != 16)
        return 4;

    return 0;
}

void tmp()
{
}

int first(int *arr)
{
    if (arr == NULL)
        return 0;

    int res = *arr;
    return res;
}

int main()
{
    int n = 10;
    int val[n];

    tmp();

    for (int i = 0; i < n; i++)
        val[i] = i + 2;

    
    if (first(val) != 2)
        return 1;

    return 0;
}
